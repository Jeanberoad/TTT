## Packages
papaparse | Client-side CSV parsing
@types/papaparse | TypeScript definitions for papaparse
react-dropzone | Beautiful drag and drop file upload
framer-motion | Page transitions and micro-interactions

## Notes
QR codes are generated using the public API: https://api.qrserver.com/v1/create-qr-code/
Print functionality relies on specific CSS `@media print` rules to hide UI and force page breaks.
